//========== APP PAGE ==========
interface INavigatorButton {
    active?: boolean
}

interface IGlobalNavbarNavContainer {
    opened: boolean
}

interface ICardModalWrapper {
    opened: boolean
}

interface IGlobalNavbarWrapper {
    opened: boolean
}